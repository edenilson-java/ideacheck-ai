// Copie este arquivo para config.js e ajuste os valores para o seu ambiente.
// config.js está no .gitignore e NÃO deve ser commitado.
window.APP_CONFIG = {
  BASE_URL: 'http://localhost:8080/api/v1',
  USE_MOCK: true,
  MOCK_DELAY_MS: 3200,
};
