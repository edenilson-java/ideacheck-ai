server:
    port: 8080

ideacheck:
    ai:
        api-key: ${GEMINI_API_KEY:}
        model: gemini-flash-latest
        base-url: https://generativelanguage.googleapis.com/v1beta

logging:
    level:
        root: WARN
        br.com.ideacheck: INFO
